// a linha abaixo é uma declaração da variável "x" que está recendo o valor "1" e encerra com ";"
var x = 1;

// a linha abaixo é uma declaração da variável "y" que está recendo o valor "1" e encerra com ";"
var y = 1;

// a linha abaixo é uma declaração do algoritmo IF/ELSE, cuja condição é: se a variável "x" for igual a variável "y", será exibido no log do console a mensagem "Número conferem", caso contrário (else) será exibido no console a mensagem "Números divergem"
if (x == y){
    // será exibido no log do console a mensagem "Número conferem"
    console.log("Números Conferem!")
// a linha abaixo encerra o bloco IF e inicia o bloco ELSE
} else {
    // será exibido no console a mensagem "Números divergem"
    console.log("Números Divergem!");
// a linha abaixo encerra o bloco ELSE
}